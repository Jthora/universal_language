Consider the following:

---
g_ij(λ)∂ᵢ∂ⱼKL(p_λ‖p₀) + (1/ℓ) ∫[λ*(θ)λ(θ')dθ']² dθ + (ζ/2) ∫[λ*(θ)∇²_Mλ(θ')dθ'] dθ − (r²/2) ∫[λ*(θ)λ(θ')Γ²(θ')dθ'] dθ = (1/√(2π)) ∑[n=1 to ∞] (1/n) ∫[λ*(θ)λ(θ')e^(i2πnθ/Θ)dθ'] dθ + (1/2) ∑[n=1 to ∞] (1/n²) ∫[λ*(θ)∇_Mλ(θ')e^(i2πnθ/Θ)dθ'] dθ

(Lᵍ × (Λ₃ + R)) − (ζ / β_a) →∇→ A∞
• Lᵍ: logos field (+2thûrial)
• Λ₃: 3-scale abstraction lattice
• R: Riemannian logic
• ζ: semantic friction, β_a: abstraction bandwidth
• ∇: covariant derivative from geometric layer

iζ D_θλ = (ζ² / 2r) D_θ²λ + g_loc |λ|²λ + g_glob F(θ)λ
  + (P₁ ∗ λ) + D_θ†(P₂ ∗ D_θ′λ) + f_abs(θ,τ) − i ω(ζ,β_a) λ

where:
• D_θ = ∂_θ − i(r/ζ)Γ(θ,τ)
• F(θ) = ∫₀ᶿ |λ|² dθ
• mₙ = 2πn / Θ
• f_abs(θ,τ) = Σₙ≥1 [(aₙ(τ)/n) λ̂ₙ + (bₙ(τ)/n²)(imₙ) λ̂ₙ] e^{imₙθ}
• λ̂ₙ = (1/Θ) ∫₀ᶿ e^{-imₙθ′} λ(θ′) dθ′
---

Now respond to the following:

Describe how a new scientific concept (e.g., 'gene') goes from being a vague intuition to a precise formal object. What are the stages of this meaning-formation process, and what drives the transition between them?